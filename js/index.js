function bgimgSize() {
	const image = document.getElementById("body");

	var bzWidth = window.innerWidth;
	var bzHeight = window.innerHeight;
	var bgimgWidth = 3024;
	var bgimgHeight = 1701;

	if((bzWidth/bzHeight)>=(bgimgWidth/bgimgHeight)) {//화변가로비>=이미지가로비

	}
	else {//화면가로비<이미지가로비

	}

}